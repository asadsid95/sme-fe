import ForgotPassword from "./forgotpassword"
import Login from "./login"
import Signup from "./signup"

export default function Modal() {

    // might be used if user auth is to occur in specific URL segment instead of root

    return <>


    </>
}